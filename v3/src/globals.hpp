#pragma once

#include <iostream>
#include <vector>
#include <string>
#include <algorithm>
#include <thread>
#include <chrono>
#include <ranges>
#include <cmath>

static int cols = 100;
static int rows = cols/2;

inline std::vector<std::string> frame;
